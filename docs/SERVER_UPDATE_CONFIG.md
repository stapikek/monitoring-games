# Настройка автоматического обновления информации о серверах

## Периодичность обновлений:

### 1. **Обновление на стороне клиента (JavaScript)**
- **Главная страница** (`index.php`): каждые **5 минут**
- **Страница сервера** (`server.php`): каждые **5 минут**
- При загрузке страницы: **немедленное** обновление

### 2. **Обновление в базе данных (Cron)**
- Рекомендуемая периодичность: **каждые 5 минут**
- Скрипт: `/api/update_all_servers.php`

---

## Настройка Cron для обновления БД

### Способ 1: Автоматическая настройка (рекомендуется)

```bash
cd /path/to/site
php setup_cron.php --install
```

Скрипт автоматически настроит все необходимые cron задачи!

### Способ 2: Через crontab (вручную)

```bash
# Открыть редактор crontab
crontab -e

# Добавить строку для обновления каждые 5 минут
# Замените /path/to/site на реальный путь к вашему сайту
*/5 * * * * /usr/bin/php /path/to/site/api/update_all_servers.php >> /path/to/site/logs/server_update.log 2>&1
```

### Способ 3: Через панель управления хостингом

Если у вас есть панель управления (ISPmanager, cPanel и т.д.):

1. Найдите раздел "Планировщик задач" или "Cron"
2. Создайте новое задание
3. **Команда:**
   ```bash
   /usr/bin/php /path/to/site/api/update_all_servers.php
   ```
   *Замените `/path/to/site` на реальный путь к вашему сайту*
4. **Периодичность:** Каждые 5 минут
5. **Формат cron:** `*/5 * * * *`

### Способ 4: Ручной запуск для тестирования

```bash
cd /path/to/site
php api/update_all_servers.php
```
*Замените `/path/to/site` на реальный путь к вашему сайту*

---

## Мониторинг обновлений

### Проверка логов:

```bash
# Просмотр последних обновлений
tail -f /path/to/site/logs/server_update.log

# Просмотр последних 50 строк
tail -n 50 /path/to/site/logs/server_update.log
```
*Замените `/path/to/site` на реальный путь к вашему сайту*

### Проверка работы cron:

```bash
# Проверить список задач cron
crontab -l

# Проверить статус cron сервиса
systemctl status cron
# или
service cron status
```

---

## Рекомендуемые настройки

### Для маленького сайта (< 50 серверов):
- **Cron:** каждые 5 минут
- **JavaScript:** каждые 5 минут
- Нагрузка: низкая

### Для среднего сайта (50-200 серверов):
- **Cron:** каждые 5 минут
- **JavaScript:** каждые 5 минут
- Нагрузка: средняя

### Для большого сайта (> 200 серверов):
- **Cron:** каждые 3 минуты
- **JavaScript:** каждые 10 минут
- Рекомендуется оптимизация запросов

---

## Изменение интервала обновления

### Изменить интервал JavaScript:

**Файл:** `assets/js/common.js`
```javascript
// Изменить значение (в миллисекундах)
setInterval(function() {
    updateAllServers();
}, 300000); // 5 минут = 300000 ms
```

**Файл:** `assets/js/server.js`
```javascript
// Изменить значение (в миллисекундах)
setInterval(function() {
    updateServerPageInfo(serverId);
}, 300000); // 5 минут = 300000 ms
```

### Примеры интервалов:
- 1 минута = `60000` мс
- 3 минуты = `180000` мс
- 5 минут = `300000` мс
- 10 минут = `600000` мс

### Изменить интервал Cron:

```bash
# Каждые 1 минуту
* * * * * php /path/to/update_all_servers.php

# Каждые 3 минуты
*/3 * * * * php /path/to/update_all_servers.php

# Каждые 5 минут
*/5 * * * * php /path/to/update_all_servers.php

# Каждые 10 минут
*/10 * * * * php /path/to/update_all_servers.php
```

---

## Диагностика проблем

### Проблема: Информация не обновляется

**Проверьте:**
1. Работает ли cron:
   ```bash
   systemctl status cron
   ```

2. Есть ли ошибки в логах:
   ```bash
   tail -n 50 /path/to/site/logs/server_update.log
   ```

3. Доступны ли серверы:
   ```bash
   cd /path/to/site
   php api/update_all_servers.php
   ```
   *Замените `/path/to/site` на реальный путь к вашему сайту*

4. Проверьте консоль браузера (F12) на ошибки JavaScript

### Проблема: Высокая нагрузка на сервер

**Решение:**
1. Увеличьте интервал обновления до 10 минут
2. Используйте кеширование (уже настроено)
3. Оптимизируйте индексы БД (уже настроено)

### Проблема: Некоторые серверы не обновляются

**Причины:**
1. Сервер недоступен (выключен, firewall)
2. Неправильный IP или порт
3. Сервер не отвечает на Steam Query

**Проверка конкретного сервера:**
```bash
php -r "
require 'config/database.php';
require 'config/server_query.php';
\$query = new ServerQuery();
\$info = \$query->getServerInfo('IP', PORT);
print_r(\$info);
"
```

---

## 📈 Оптимизация производительности

### Текущие настройки (после оптимизации):
- Составные индексы для быстрых запросов
- Кеширование статических данных (1 час)
- Оптимизированные JOIN запросы
- Ограничение выборки (LIMIT)
- Обновление раз в 5 минут

### Ожидаемые показатели:
- Время выполнения update_all_servers.php: 10-30 секунд (для 50 серверов)
- Нагрузка на БД: < 20%
- Использование RAM: < 500MB
- Queries per second: 100-500

---

## Чек-лист настройки:

- [ ] Настроен cron для обновления каждые 5 минут
- [ ] Проверены логи обновлений
- [ ] JavaScript обновляется каждые 5 минут
- [ ] Создана директория для логов (`mkdir -p logs`)
- [ ] Проверена доступность серверов
- [ ] Оптимизированы индексы БД
- [ ] Настроено кеширование

---